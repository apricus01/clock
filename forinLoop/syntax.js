
// const arr=[1,2,3,4,];

// for(key in arr){
    //     console.log(key)
    // }
    const myob={
        js:'javascript',
        cpp:'c++',
        rb:'rubby',
    }
    
    for (const key in myob){
        console.log(`${key} is the shortcut name of ${myob[key]}`)
    }

